class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        Hash = set()
        while head is not None:
            if head in Hash:
                return True
            Hash.add(head)
            head = head.next
        return False
